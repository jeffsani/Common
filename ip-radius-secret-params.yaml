--- 
name: ip-radius-secret-params
namespace: com.citrix.adc.commontypes
version: "1.0"
description: "This stylebook defines radius node client-server secrets"
private: true
schema-version: "1.0"
parameters: 
   - 
      name:  network
      label: "Client or Server Network"
      description: "client or server subnet in CIDR format "
      type: ipaddress
      required: true
   - 
      name: secret
      label: "Node Secret"
      description: "Radius Secret for defined network"
      type: password
      required: true
   - 
      name: add-node-secret
      label: "Add node-secret mapping"
      description: "Option to add a radius node secret entity explicity and be managed by the stylebook"
      type: boolean
      default: true
      gui:
        hidden: true
